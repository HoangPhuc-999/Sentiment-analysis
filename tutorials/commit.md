# Commit code

```
- git pull # pull latest changes, may have conflict
- edit code 
- git add <file1.py> <file2.py> (also: git add .)
- git commit -m "messages" (message in Vietnamese) # for first-time commit, you have to config your username and your email!
- git push 
```
